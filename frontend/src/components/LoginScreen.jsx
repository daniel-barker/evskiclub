import React from "react";
// If a user attempts to access a protected route, they will be brought here and then redirected back to that route after they login.

const LoginScreen = () => {
  return <div>LoginScreen</div>;
};

export default LoginScreen;
